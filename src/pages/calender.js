export default function Calender() {
  return (
    <section className="page-card">
      <h3>Calendar</h3>
      <p>
        Session management is client-side with browser storage and token validation on page load.
      </p>
    </section>
  );
}
