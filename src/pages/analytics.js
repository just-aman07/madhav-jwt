export default function Analytics() {
  return (
    <section className="page-card">
      <h3>Analytics</h3>
      <p>
        JWT-based sessions keep the server stateless by validating the token on each request.
      </p>
    </section>
  );
}
